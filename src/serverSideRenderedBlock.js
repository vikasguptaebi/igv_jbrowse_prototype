function renderBlockData(block) {
    if (!block || !block.data || !Array.isArray(block.data) || !block.data.includes) {
        console.error('Block data is undefined or malformed:', block);
        return null;
    }

    // Rest of the function logic
    // ...
}
